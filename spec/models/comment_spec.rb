require 'rails_helper'

RSpec.describe Comment, type: :model do

  # before(:each) do
  #   Article.destroy_all
  # end

  # let(:article) { Article.new }
  # # let(:comment) { Comment.new }

  # context 'with 2 or more comments in an Article' do
  #   it 'orders them in reverse chronologically' do
  #     article = Article.create!(name:'Article name', body:'Article Body')
  #     comment1 = article.comments.create(body:"first comment")
  #     comment2 = article.comments.create(body:"second comment")
      
  #     expect(Article.last.comments).to eq([comment2, comment1])
  #     expect(Article.last.comments).to be_valid
  #   end
  # end
end
