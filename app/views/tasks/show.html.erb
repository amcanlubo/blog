<p id="notice"><%= notice %></p>

<p>
  <strong>Category:</strong>
  <%= @task.category_id %>
</p>

<p>
  <strong>Name:</strong>
  <%= @task.name %>
</p>

<p>
  <strong>Body:</strong>
  <%= @task.body %>
</p>

<p>
  <strong>Task date:</strong>
  <%= @task.task_date %>
</p>

<%= link_to 'Edit', edit_category_task_path(@category, @task) %> |
<%= link_to 'Delete', category_task_path(@category, @task), method: :delete, data: { confirm: 'Are you sure?' } %></td> |
<%= link_to 'Back to categories', @task.category %> |
<%# <%= link_to 'Back', category_path(@category.id) %> 
<%= link_to 'Back to main', categories_path %>
