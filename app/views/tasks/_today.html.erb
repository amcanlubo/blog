<div class="h-auto bg-yellow-500 text-black m-5 py-5 px-10 rounded-md absolute top-0 left-0">
  <h1 class='text-2xl text-center font-bold'>ALL TASKS FOR TODAY</h1>
  <p class='text-center'><%= DateTime.now.strftime('%a, %d/%b/%Y') %></p>
  <br>
  <%# <% if @task.any? %>  
  <table>
    <thead>
      <tr>
      <th class='text-center'>Category</th>
        <th class='text-center'>Name</th>
        <th class='text-center'>Body</th>
        <th class='text-center'>Task date</th>
      </tr>
    </thead>

    <tbody>
    <% @tasks.each do |task| %>    
          <tr>
            <td class='text-center'><%= task.category_id %></td>
            <td class='text-center'><%= task.name %></td>
            <td class='text-center'><%= task.body %></td>
            <td class='text-center'><%= task.task_date.strftime('%a, %d/%b/%Y') %></td>  
    <%# <% else %>
        <%# <h1>No tasks today :( </h1> %>
    <%# <% end %> 
    <% end %>  
        </tr>
    </tbody>
  </table>
</div>