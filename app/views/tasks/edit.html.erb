<div class='p-5'>
<h1 class='font-bold text-lg'>EDIT TASK</h1>

<%= render 'form', task: @task %>

<%= link_to '<< Back', @task.category %>
<%# <%= link_to 'Back', category_path(@category.id) %> 
<%= link_to 'Show', category_task_path(@category.id, @task.id) %>

</div>